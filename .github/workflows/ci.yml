name: CI/CD

on:
  push:
    branches:
      - '**'
    tags:
      - '[0-9]+.[0-9]+.[0-9]+'
  pull_request:
    branches:
      - '**'

env:
  JAVA_VERSION: '17'
  JAVA_DISTRIBUTION: 'temurin'

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest

    outputs:
      semver: ${{ steps.gitversion.outputs.semVer }}
      fullsemver: ${{ steps.gitversion.outputs.fullSemVer }}
      major: ${{ steps.gitversion.outputs.major }}
      minor: ${{ steps.gitversion.outputs.minor }}
      patch: ${{ steps.gitversion.outputs.patch }}
      prerelease: ${{ steps.gitversion.outputs.preReleaseTag }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Full history required for GitVersion

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v0
        with:
          versionSpec: '5.x'

      - name: Determine Version
        id: gitversion
        uses: gittools/actions/gitversion/execute@v0
        with:
          useConfigFile: true
          configFilePath: GitVersion.yml

      - name: Display GitVersion outputs
        run: |
          echo "SemVer: ${{ steps.gitversion.outputs.semVer }}"
          echo "FullSemVer: ${{ steps.gitversion.outputs.fullSemVer }}"
          echo "Major: ${{ steps.gitversion.outputs.major }}"
          echo "Minor: ${{ steps.gitversion.outputs.minor }}"
          echo "Patch: ${{ steps.gitversion.outputs.patch }}"
          echo "PreReleaseTag: ${{ steps.gitversion.outputs.preReleaseTag }}"

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}
          cache: maven

      - name: Update pom.xml version
        run: |
          mvn versions:set -DnewVersion=${{ steps.gitversion.outputs.semVer }} -DgenerateBackupPoms=false
          echo "Updated pom.xml to version ${{ steps.gitversion.outputs.semVer }}"

      - name: Run tests
        run: mvn clean test

      - name: Build JAR
        run: mvn package -DskipTests

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: keycloak-maxmind-${{ steps.gitversion.outputs.semVer }}
          path: target/*.jar
          retention-days: 5

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: target/surefire-reports/
          retention-days: 5

  develop-release:
    name: Develop Pre-Release
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v0
        with:
          versionSpec: '5.x'

      - name: Determine Version
        id: gitversion
        uses: gittools/actions/gitversion/execute@v0
        with:
          useConfigFile: true
          configFilePath: GitVersion.yml

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}
          cache: maven

      - name: Update pom.xml version
        run: mvn versions:set -DnewVersion=${{ steps.gitversion.outputs.semVer }} -DgenerateBackupPoms=false

      - name: Build JAR
        run: mvn clean package -DskipTests

      - name: Rename JAR
        run: |
          mv target/zymlabs-maxmind-provider.jar target/zymlabs-maxmind-provider-${{ steps.gitversion.outputs.semVer }}.jar

      - name: Delete old pre-releases
        uses: actions/github-script@v7
        with:
          script: |
            const releases = await github.rest.repos.listReleases({
              owner: context.repo.owner,
              repo: context.repo.repo,
            });

            const preReleases = releases.data.filter(r => r.prerelease && r.tag_name.includes('alpha'));

            // Keep only the 5 most recent pre-releases
            const toDelete = preReleases.slice(5);

            for (const release of toDelete) {
              console.log(`Deleting old pre-release: ${release.tag_name}`);
              await github.rest.repos.deleteRelease({
                owner: context.repo.owner,
                repo: context.repo.repo,
                release_id: release.id,
              });

              // Also delete the tag
              try {
                await github.rest.git.deleteRef({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  ref: `tags/${release.tag_name}`,
                });
              } catch (error) {
                console.log(`Could not delete tag ${release.tag_name}: ${error.message}`);
              }
            }

      - name: Create Pre-Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.gitversion.outputs.semVer }}
          name: Development Build ${{ steps.gitversion.outputs.semVer }}
          body: |
            **Automated development build from `develop` branch**

            - Version: `${{ steps.gitversion.outputs.semVer }}`
            - Commit: ${{ github.sha }}
            - Branch: ${{ github.ref_name }}

            This is a pre-release build for testing purposes. Use stable releases for production.
          files: target/zymlabs-maxmind-provider-${{ steps.gitversion.outputs.semVer }}.jar
          prerelease: true
          draft: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  tagged-release:
    name: Production Release
    runs-on: ubuntu-latest
    needs: build-and-test
    if: startsWith(github.ref, 'refs/tags/') && github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install GitVersion
        uses: gittools/actions/gitversion/setup@v0
        with:
          versionSpec: '5.x'

      - name: Determine Version
        id: gitversion
        uses: gittools/actions/gitversion/execute@v0
        with:
          useConfigFile: true
          configFilePath: GitVersion.yml

      - name: Validate tag matches version
        run: |
          TAG_VERSION="${GITHUB_REF#refs/tags/}"
          GITVERSION="${{ steps.gitversion.outputs.semVer }}"

          if [ "$TAG_VERSION" != "$GITVERSION" ]; then
            echo "Error: Tag version ($TAG_VERSION) does not match GitVersion ($GITVERSION)"
            exit 1
          fi

          echo "Version validated: $TAG_VERSION"

      - name: Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}
          cache: maven

      - name: Update pom.xml version
        run: mvn versions:set -DnewVersion=${{ steps.gitversion.outputs.semVer }} -DgenerateBackupPoms=false

      - name: Build JAR
        run: mvn clean package -DskipTests

      - name: Rename JAR
        run: |
          mv target/zymlabs-maxmind-provider.jar target/zymlabs-maxmind-provider-${{ steps.gitversion.outputs.semVer }}.jar

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Release ${{ steps.gitversion.outputs.semVer }}
          body: |
            **Production release ${{ steps.gitversion.outputs.semVer }}**

            See [CHANGELOG.md](CHANGELOG.md) for details.

            ## Installation

            Download `zymlabs-maxmind-provider-${{ steps.gitversion.outputs.semVer }}.jar` and copy to your Keycloak providers directory:

            ```bash
            cp zymlabs-maxmind-provider-${{ steps.gitversion.outputs.semVer }}.jar /path/to/keycloak/providers/
            bin/kc.sh build
            bin/kc.sh start
            ```
          files: target/zymlabs-maxmind-provider-${{ steps.gitversion.outputs.semVer }}.jar
          prerelease: false
          draft: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
